# Marketing Claims Register

Every public claim about Vura, with how it is verified. Rule: a claim ships
only with a row here; a row needs a command someone else can run, or it's cut.
"Faster than X" without a reproducible benchmark is banned outright.

| # | Claim | Where it appears | Verification | Status |
|---|---|---|---|---|
| 1 | "MIT, forever — no relicensing" | README.md, GOVERNANCE.md | `cat LICENSE` — MIT header; `grep -L '"license": "MIT"' packages/*/package.json` must return empty | verified |
| 2 | "Every self-host guide is executed in CI on every commit" | docs-site `/self-host` index | `.github/workflows/selfhost.yml` — six jobs (node, docker, fly, railway-via-docker, cloudflare workerd, lambda sam-local), green on every PR; last confirmed on vura#101, 2026-08-25. Railway caveat (the docker job is its CI coverage) is disclosed on the railway guide page itself. **Stale copy flag cleared 2026-08-25:** the `/self-host` index now says "Every guide below runs in CI on every commit" with a link to the workflow, and no longer claims manual verification. | verified |
| 3 | "Nothing is platform-gated — websockets, cache revalidation, tasks, cron all work fully self-hosted — verified by an automated audit suite in CI" | GOVERNANCE.md | `pnpm test:selfhost-audit` — `tests/self-host-audit/no-platform-gating.test.ts`, assertions A0–A9; `selfhost-audit` CI job in `.github/workflows/ci.yml` runs on every commit | verified |
| 4 | "Static pages ship zero framework JavaScript" | README.md (`static` pages section), docs-site scaffold copy | `pnpm test:selfhost-audit` — `tests/self-host-audit/static-output.test.ts` (S1) builds a real project and asserts a static page references no `_then/` bundle and carries no executable `<script>`. The one script tag a static page may carry is the loader payload, `type="application/json"`, which the browser parses as data and never executes. The suite also asserts client and hybrid pages DO ship a bundle, so the check cannot pass by the bundles disappearing, and that the page still has content, so it cannot pass by rendering nothing. | verified (CI assertion wired 2026-08-25; was `planned` since 2026-06-11) |
| 5 | "`revalidateTag` / cache invalidation works self-hosted" | GOVERNANCE.md | Scope to projects where a CDN adapter is configured; "global/CDN purge" wording only appears where CDN adapter docs exist | qualified — "global/CDN purge" wording only where a CDN adapter is configured |
| 6 | "Hot routes: no timeout, in-memory state, websockets" | docs-site `/ladder/4-hot` | `packages/core/test/hot-routes.test.ts` (15 tests as of de932e2 — was 12 when this row was written; all 15 pass, re-run 2026-06-11); "no timeout" = no framework-imposed limit; host/platform limits still apply (4-hot page documents `kill_timeout` etc.) | verified at code level (count corrected 12→15) |
| 7 | "One-line change to promote a route to hot" (kind='hot') | (future landing) | Literally one export-field change; rung-4 docs page will show the actual diff | verified |
| 8 | "Built on What Framework + CelsianJS" | README.md | `packages/core/package.json` deps: `what-framework ^0.13.2`, `what-isr ^0.13.2`, `@celsian/core ^0.5.2`, `@celsian/jwt ^0.5.2`; runtime composition in `packages/core/src/` | verified (versions re-read 2026-08-25 at 0.6.1; all 8 packages published) |
| 9 | "12 KB runtime" (What Framework) | `sites/landing/index.html` (now redirected) | This number belongs to the What Framework project, not Vura. **Not verified within this repo.** Must not appear in Vura-owned pages until confirmed by a reproducible measurement in the what-framework repo. | removed from sites/landing (redirect); **banned from Vura-owned copy until externally sourced** |
| 10 | "Static pages ship zero JavaScript" (in landing code example comment) | `sites/landing/index.html` (now redirected) | Same as row 4 — code comment in a demo snippet, inherently illustrative. Landing page replaced with redirect; if reused in future docs, must link to the CI assertion. | removed from sites/landing (redirect) |
| 11 | "Fast by default" (signal reactivity) | `sites/landing/index.html` (now redirected) | Vague superlative with no benchmark. **Banned outright** from Vura-controlled copy unless replaced with a reproducible benchmark number. | removed from sites/landing (redirect) |
| 12 | "Up and running in minutes" | `sites/landing/index.html` (now redirected) | Qualitative. Acceptable as marketing copy only if the quick-start scaffold actually completes without error — gated on v0.4.0 publish + smoke test. | removed from sites/landing (redirect) |
| 13 | Version numbers (What Framework v0.8, CelsianJS v0.3, Vura v0.1) in ecosystem cards | `sites/landing/index.html` (now redirected) | Were stale at time of redirect (What FW is 0.11.1, Celsian 0.5.2, Vura 0.4.0 on this branch). All removed with the redirect. Future landing must pull version from package.json at build time. | removed from sites/landing (redirect) |
| 14 | Canonical domain | `sites/landing/index.html`, all docs links | Canonical framework domain is **vura.io** (decided 2026-06-11; vura.app is parked and reserved). `vura.dev` is an unrelated third-party product (Postgres proxy) — never link to it. Old landing replaced with a redirect to `https://github.com/CelsianJs/vura` until the docs site takes over the vura.io deploy (A3 Task 11). | decided — vura.io |
| 15 | "Full-Stack TypeScript Framework" (title/og:title) | `sites/landing/index.html` (now redirected) | Pre-wedge positioning; Vura's current wedge is "outgrew serverless / hot routes". Removed with the redirect. | removed from sites/landing (redirect) |
| 16 | Package tarball sizes | `scripts/package-size-limits.json` | `pnpm run package:size` — tracked ceilings as of 0.6.1: core 158 KB, compiler 15 KB, CLI 54 KB, create-vura 10 KB, Cloudflare adapter 15 KB, Lambda adapter 18 KB, managed Vura adapter 15 KB, Vite plugin 16 KB. These are **ceiling limits**, not marketing numbers; do not quote them as "tiny" or "lightweight" without a real comparative baseline. | verified as limits (re-read 2026-08-25; core 150→158 KB and CLI 50→54 KB in 0.6.1) |
| 17 | "Native Rust compiler" / "native-speed alternative" | `packages/compiler-native/README.md` | Prototype only; package is private and unpublished. README correctly says "unpublished prototype — source-only until platform binaries are released." | qualified — do not surface externally until binaries ship |
| 18 | `vura deploy` for managed platform | README.md, `sites/landing/index.html` (now redirected) | README correctly states "intentionally fails closed in the open-source CLI." Landing card said "Currently in private beta." No commit gate on this claim, but it is forward-looking. | qualified — present-tense "private beta" claim must not appear until beta access is open; README wording is correct |
| 19 | Test counts | (used in internal planning docs) | `pnpm test 2>&1 \| grep 'Tests '` — **949 unit tests** across 76 files, plus 57 self-host audit assertions (`pnpm test:selfhost-audit`) as of 2026-08-25. Recount at each release; do not embed a static count in public copy | not a public claim; recorded here to prevent quoting without a recount |
| 20 | AI-native / MCP DevTools | `sites/landing/index.html` (now redirected) | Belongs to What Framework, not Vura core. Removed with redirect. Acceptable in future Vura copy only as "What Framework's MCP DevTools are accessible from Vura projects" with a link to what-framework docs. | removed from sites/landing (redirect) |
| 21 | "create-vura emits twelve files" | `docs-site/pages/ladder/0-create.md` | `node packages/create-vura/dist/index.js /tmp/verify-0create --no-install 2>&1 \| grep -c '+'` — must return 12. **Corrected 2026-08-25:** the command returned 12 while the docs said eleven and its file tree omitted `src/styles.ts`. Both fixed. Running this row's own command also revealed that `create-vura` sanitized the whole argument as a package name, deleting the slashes, so it wrote `tmpverify-0create` into the current directory instead of `/tmp/verify-0create`. The argument is now a target path. | verified by execution 2026-08-25 |
| 22 | "Static pages: `dist/static/<page>/index.html`, zero JS shipped" | `docs-site/pages/ladder/1-static.md` | Same gate as row 4: `tests/self-host-audit/static-output.test.ts` (S1). | verified (CI assertion wired 2026-08-25; was `planned` since 2026-06-11) |
| 23 | "`revalidate` and `tags` are flat properties on the page config (not nested)" | `docs-site/pages/ladder/2-cache.md` | `packages/core/src/runtime/pages.ts` lines 93–103: `typeof p.config.revalidate === 'number'` and `p.config.tags` — both read from flat config, not from a nested `revalidate` object | verified — code-level |
| 24 | "`vura tasks run <name>` outputs JSON with status/result/attempts" | `docs-site/pages/ladder/5-tasks.md` | `packages/cli/src/commands/tasks.ts` line 102: `console.log(JSON.stringify(result, null, 2))` where result is `TaskRunResult` | verified — code-level |
| 25 | "Hot routes cannot run on Cloudflare Workers or AWS Lambda" | `docs-site/pages/ladder/6-deploy.md` | `packages/adapter-cloudflare/src/index.ts` line 496 (was 498; drifted): `manifest.api.filter(r => r.kind === 'serverless')` — only serverless routes are bundled; hot routes trigger an explicit build warning pointing at /self-host/ | verified — code-level |
| 26 | "Task cron on Cloudflare Workers uses the `scheduled` event" | `docs-site/pages/ladder/5-tasks.md`, `6-deploy.md` | `packages/adapter-cloudflare/src/index.ts` lines 408–428: `scheduled(event, env, ctx)` handler in generated Worker entry; `wrangler.toml` cron triggers from `generateWranglerToml` | verified — code-level |
| 27 | "This is the zero-config path: no `dist/` to manage, no server to provision" (`vura deploy` managed platform) | `docs-site/pages/ladder/6-deploy.md` | Found in Task 11 register sweep (unregistered superlative). Same paragraph immediately discloses the command "intentionally fails closed in the open-source CLI" — same gate as row 18. "Zero-config" describes a path that is not yet publicly usable, so it is unverifiable today. | qualified — flagged at sign-off: remove or verify when beta opens; acceptable interim only because the fail-closed disclosure is in the same paragraph |
| 28 | "A file under `src/actions/` runs on the server, always; the browser never receives the module" | `docs-site/pages/reference/actions.md` | `pnpm test:selfhost-audit`, `tests/self-host-audit/actions.test.ts` (V2) builds a real project whose action file holds a credential-shaped constant and a `node:fs` import, then asserts that string appears in **no** file under `dist/static`, and that the page bundle carries the action id and the fetch endpoint instead. `packages/core/test/actions.test.ts` (A9) proves the esbuild plugin itself, including a symlinked project root, the form in which the containment check originally failed open. | verified by execution 2026-08-25 |
| 29 | "Throw an `HttpError` and the caller receives that status and message" (server actions) | `docs-site/pages/reference/actions.md` | `pnpm test:selfhost-audit`, `tests/self-host-audit/actions.test.ts` (V5) calls an action that throws `notFound()` in a **built** app and asserts a 404 with the message, and an action that throws a plain `Error` containing a connection string and asserts a sanitised 500 that does not contain it. Verified adversarially: replacing the structural check with `instanceof` turns the 404 into a 500, because each server bundle inlines its own copy of core. | verified by execution 2026-08-25 |
| 30 | "Same-origin only / JSON only / CSRF token" (server-action endpoint) | `docs-site/pages/reference/actions.md` | `tests/self-host-audit/actions.test.ts` (V4) drives a booted server over HTTP: missing token, token not matching the cookie, `Sec-Fetch-Site: cross-site`, and a form-encoded body are each rejected (403/403/403/415), and a cross-origin token request is refused. `packages/core/test/actions.test.ts` (A4–A7) covers the branches, including a request carrying neither `Origin` nor `Sec-Fetch-Site`. | verified by execution 2026-08-25 |
| 31 | "Server actions work in `vura dev` and in a built Node server; not yet on the Cloudflare or Lambda adapters" | `docs-site/pages/reference/actions.md` (Where actions run) | The two supported rows are covered by the audit suite (built server) and by manual `vura dev` verification on 2026-08-25: token issued, action executed, `notFound()` returned 404, and the dev browser bundle contained the stub and not the source. The two "not yet" rows are the honest state — the adapters bundle API routes only. Re-check when adapter support lands. | verified for the rows claimed 2026-08-25 |
| 32 | "Each server module is bundled separately, so a module-level value is not shared between them" (and the three generated `package.json` files) | `docs-site/pages/reference/build-output.md` | The split is what `tests/self-host-audit/loaders.test.ts` (L5) and `actions.test.ts` (V1) observe directly: L5 asserts `dist/package.json` pins what-framework to the version the project installed (the inner `dist/server/` and `dist/functions/` manifests carry `type` only, which is why the Docker guide installs from `dist/`), and V1 asserts action modules are emitted under `dist/server/actions/`. The consequence the page warns about is pinned by the two runtime fixes that needed it: `packages/core/test/loader.test.ts` and `actions.test.ts` (A3) both assert the shared object is reachable through a `Symbol.for` key on globalThis rather than a module-level binding. The page's own diagnostic command was run against a real build: `grep -c 'from "what-framework"' dist/server/pages/*.js` returns 4, and the same grep against `entry.js` returns 0. | verified by execution 2026-08-25 |
| 33 | "The browser receives `<head>` and everything up to the `<Suspense>` boundary immediately" (streaming SSR) | `docs-site/pages/reference/streaming.md` (Streaming a page) | `tests/self-host-audit/streaming.test.ts` (S1) reads the socket chunk by chunk against a built and booted app and requires the shell marker to arrive more than 40 ms before the resolved data of a 150 ms resource, across more than one chunk. Verified adversarially: with the streaming branch disabled the resolved data does not appear at all, because a synchronous render cannot await it. | verified by execution 2026-08-25 |
| 34 | "`vura dev` serves streamed pages through the same code path as a build" | `docs-site/pages/reference/streaming.md` (What streaming does not change) | `tests/self-host-audit/streaming.test.ts` (S3) boots the real `vura dev` against the same scaffold and applies the production assertions to it: chunk timing, the loader chain, a 404 from `throw ctx.notFound()`, and that a non-streaming page is left on the buffered path. Verified adversarially: disabling only the dev branch fails S3 while the production tests stay green. | verified by execution 2026-08-25 |
| 35 | "A streamed page skips the ISR cache" and "no `content-length`, and Vura does not set `transfer-encoding`" | `docs-site/pages/reference/streaming.md` (What streaming does not change, Headers) | The cache exclusion is structural: `vuraPagesHandler` matches streaming routes before delegating to the cached handler (`packages/core/src/runtime/pages.ts`). The headers are asserted both over HTTP against a booted server (S1) and at the unit level (`packages/core/test/streaming.test.ts`), which requires `content-length` and `transfer-encoding` to be absent. | verified by execution 2026-08-25 |

---

## Packages/\*/README.md inventory (Task 10)

The following hits from `packages/*/README.md` are catalogued here for Task 10
to resolve. They are not currently public marketing claims (packages not yet
published at v0.4.0) but must be cleaned before publish.

| Ref | File | Hit | Assessment |
|---|---|---|---|
| P1 | `packages/compiler-native/README.md` | "optional native-speed alternative" | Resolved (Task 10): wording replaced with "AST-based route scanner and JSX transformer written in Rust" — describes mechanism, not a speed claim |
| P2 | `packages/compiler/README.md` | "transforms routes, pages, and API endpoints into optimized build output" | Resolved (Task 10): "optimized" removed; new wording is "regex-based route scanning and JSX transforms" — descriptive, no unmeasured superlatives |

---

## Sign-off

Signed off: claims-review agent (independent), 2026-06-11, vura v0.4.0, docs-site commit de932e2 — all rows verified against the built site; vura.io cutover pending merge.

Signed off: v0.5.0, 2026-06-12 — new-in-0.5 claims (dev WebSockets, origins allowlist, cache config wiring + env-only secrets, req.parsedQuery, A0–A12 audit) verified empirically by the release smoke-audit (fresh scaffold from packed tarballs, real ws frames, cache HIT across process restart, browser-checked docs) attached to PR #25; vura.io live with the v0.5.0 badge.

Signed off: v0.5.14, 2026-07-16 — no new marketing claims; managed Function/Dedicated placement wording, the 15 KB managed-adapter ceiling, and the v0.5.14 docs badge were checked against packed artifacts, the 775-test suite, the A0–A12 self-host audit, the built 30-page docs site, and the recursive link scan.

Signed off: v0.7.0, 2026-08-25. New-in-0.7 claims (rows 28 to 32: server actions run server-side and never reach a browser bundle, HttpError statuses survive the bundle boundary, the endpoint's same-origin/JSON/CSRF gates, the honest per-target support table, and the build-output/bundling model) verified by execution against a real built and booted scaffold, plus a manual browser pass in `vura dev` and against a production build. Rows 4, 19, 21, 22 re-read; row 19 recounted to 956 unit tests and 62 audit assertions. Row 16 ceilings updated for core (182 KB) and the CLI (56 KB).

Signed off: streaming SSR, 2026-08-25. Rows 33 to 35 verified by execution against a built and booted scaffold and against a real `vura dev`, each one adversarially re-checked by disabling the code path under test and confirming the assertion fails. Row 19's counts are stale again and are recounted at the next release.
