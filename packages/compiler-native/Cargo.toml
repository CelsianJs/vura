[package]
name = "then-compiler-native"
version = "0.1.0"
edition = "2021"
description = "Native Rust compiler for ThenJS — route scanning, JSX transforms, and file watching"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["napi4", "serde-json"] }
napi-derive = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
swc_ecma_parser = "0.149"
swc_ecma_ast = "0.118"
swc_common = { version = "0.37", features = ["sourcemap"] }
notify = { version = "6", features = ["macos_kqueue"] }

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
strip = "symbols"
opt-level = 3
