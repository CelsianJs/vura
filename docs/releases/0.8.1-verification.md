# Vura 0.8.1 verification record

Verifier: Codex agent, 2026-09-05. This is a scoped evidence record, **not a
completed full smoke-audit or a human product acceptance**.

## Candidate evidence

Candidate: `0d3d29479e307dfc61e67e415dbe694e54303576` (merged PR #139).
GitHub's check-runs API was read for that exact commit; all 13 returned checks
were completed with conclusion `success`:

- [Node 20, Node 22, and selfhost-audit](https://depot.dev/orgs/wbjkqlz33v/workflows/bxddz860sk)
- [Scaffold and six deployment-guide targets](https://depot.dev/orgs/wbjkqlz33v/workflows/80p9fdfmpc)
- [Docs build, link check, and version assertion](https://depot.dev/orgs/wbjkqlz33v/workflows/1z989psms9)
- [CodeQL and secret scan](https://depot.dev/orgs/wbjkqlz33v/workflows/km8j5m8f7m)

[The candidate release-check evidence](https://github.com/CelsianJs/vura/pull/139#issuecomment-5554616466)
records 1,152 tests across 93 files, production dependency audit, publish
verification/dry-run, all eight package-size budgets, and a clean-tree pass.
The retained local log ends with `OK: release tree is clean` and
`OK: release check passed`. The packed self-host suite passed 85/85 assertions.
The local run used Node 22, four test workers, and a fresh temporary npm cache;
no assertions or timeouts were relaxed. Unrestricted local parallelism had
previously produced timeouts; that failed attempt is not treated as a pass.

## Live documentation links

All 11 distinct `https://vura.io` URLs extracted from the root and package
READMEs were fetched with GET, no-store cache preference, and a 20-second timeout.
All returned HTTP 200 on 2026-09-05 with these destination headings. This includes
the eight published-package READMEs and the private compiler prototype README.

| URL path | Destination heading |
|---|---|
| `/` | The framework for apps that outgrow serverless. |
| `/self-host/` | Self-host guides |
| `/self-host/cloudflare/` | Cloudflare Workers |
| `/self-host/lambda/` | AWS Lambda |
| `/ladder/0-create/` | Rung 0 — Create: your first running app |
| `/ladder/5-tasks/` | Rung 5 — Background task: work off the request path |
| `/reference/cli/` | CLI reference |
| `/reference/config/` | vura.config reference |
| `/ladder/3-api/` | Rung 3 — API route: a backend endpoint |
| `/ladder/2-cache/` | Rung 2 — Server + cache: fresh content without rebuilding |
| `/reference/route-kinds/` | Route kinds and compute classes |

The live homepage contains `v0.8.1`. No documentation URL in this set has a
fragment. README labels that promised different content (auth helpers, compiler
internals, platform overview, and obsolete reference paths) were corrected in the
release-policy follow-up; duplicate links to the same scaffold guide were removed.
The URLs themselves did not change. npm badges, npm package pages, and unrelated
external URLs are not part of this documentation-URL result.

The existing docs CI excludes external GitHub URLs. It must not be presented as
proof of every external link or subjective visual quality.

## Changelog review

The 0.8.1 entry was compared with `git log v0.8.0..0d3d294` and the relevant
source/test changes: streaming/cache/query fixes, action containment, runtime
splits and serialization, adapter pages/hooks/auth helpers, Railway, docs redirects,
size checks, and independently released What 0.13.5 / Celsian 0.6.2. It records
unsupported adapter middleware/actions and persistent-cache upgrade requirements.
This is factual release-note review, not a claim of enterprise/GA readiness.

## Still agent-owned, not human gates

- Complete and attach the full packed-candidate **and browser/docs** smoke-audit;
  the passing automated tests above alone do not close that combined requirement.
- Refresh and verify `CLAIMS.md` for 0.8.1. It still has stale upstream versions,
  guide coverage, test counts, and adapter explanation; self-hosted durable-task
  semantics and managed-platform availability claims need reconciliation with
  actual behavior. Do not sign off unsupported claims.
- Refresh affected package checks after the README corrections. Record the new
  commit and CI results in the PR; old candidate evidence remains labeled above.
- After pre-release acceptance, tag/publish and verify all eight registry packages.
  No 0.8.1 publication is asserted here.

## Human-only acceptance

Only subjective public-facing presentation is currently requested: at
https://vura.io/, assess whether the desktop/mobile typography, spacing, navigation,
and documentation presentation meet the desired product quality. Functional browser
checks, link checks, factual claims, and release execution remain agent-owned.
No new requirement to manually repeat the automated framework tests is imposed.
