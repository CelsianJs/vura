{
  "name": "backend-only-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vura build",
    "manifest": "vura manifest",
    "deploy": "vura deploy"
  },
  "devDependencies": {
    "@then/cli": "workspace:*",
    "@then/core": "workspace:*"
  }
}
