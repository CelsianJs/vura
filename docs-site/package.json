{
  "name": "vura-docs-site",
  "private": true,
  "type": "module",
  "description": "vura.io — built with What Framework (SSG via renderToString)",
  "scripts": {
    "build": "node build.mjs",
    "build:vura": "node build.mjs --vura",
    "preview": "node build.mjs && npx --yes serve dist -l 4179"
  },
  "dependencies": {
    "what-framework": "^0.13.5",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
